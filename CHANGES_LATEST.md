# Последние изменения

## Исправлено

### 1. QR Генератор - убрана перегенерация
- Формат QR: `QR_AUTH_2026-04-02` (без уникального ID)
- Каждый раз генерируется один и тот же QR для выбранной даты
- Проще и стабильнее

### 2. Крестик в бургер-меню
- Исправлена анимация: `translate(6px, 6px)` вместо `(7px, 7px)`
- Теперь крестик центрирован правильно

### 3. Меню "РАЗДЕЛ" → "ЛОГИ"
- Заголовок изменен с "Раздел" на "Логи"
- Под заголовком кнопки "Отметки" и "Опоздания"
- На мобильных заголовок выделен фоном для лучшей видимости

## Структура меню админа

```
┌─────────────────────┐
│ ЛОГИ                │ ← Заголовок (не кликабельный)
├─────────────────────┤
│ Отметки             │ ← Кнопка
├─────────────────────┤
│ Опоздания           │ ← Кнопка
├─────────────────────┤
│ Пользователи        │ ← Кнопка
├─────────────────────┤
│ Генератор           │ ← Кнопка
└─────────────────────┘
```

## Формат QR кода

Старый (с перегенерацией): `QR_AUTH_2026-04-02_12847`
Новый (простой): `QR_AUTH_2026-04-02`

## Файлы обновлены

- `app.js` - убрана генерация уникального ID, исправлено меню
- `style.css` - исправлен крестик, стили для заголовка раздела

Все файлы скопированы в `C:\Users\Nishiki\Documents\GitHub\office_qr_auth\`
